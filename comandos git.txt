
# deploy Desde la RAÍZ del proyecto
npm i
npm run build
netlify deploy --prod --dir=dist --functions=netlify/functions --skip-functions-cache








# 1) Guardar el archivo
git add src/pages/Asistencia.jsx
git commit -m "fix(asistencia): validación acuse RH en tiempo real + mejoras UX"

# 2) Build local (opcional)
npm run build

# 3) Desplegar a producción en Netlify (con funciones sin caché si tocaste serverless)
netlify deploy --prod --dir=dist --functions=netlify/functions --skip-functions-cache





git remote remove origin 2>$null
git remote add origin https://github.com/gersolano/sityps-landing.git
git push -u origin main








o initialize a new git repo follow the steps below.

1. Initialize a new repo:

   git init

2. Add your files

   git add .

3. Commit your files

   git commit -m 'initial commit'

4. Create a new repo in GitHub https://github.com/new

5. Link the remote repo with this local directory

   git remote add origin git@github.com:YourGithubName/your-repo-slug.git

6. Push up your files

   git push -u origin main

7. Initialize your Netlify Site

   netlify init


token netlify
nfp_BfKFKfnaYzaZBUTjzYBFxULjybK1zh7ac0cc

onst name = process.env.BLOBS_STORE_NAME || 'sityps';
  const siteID = process.env.NETLIFY_SITE_ID || process.env.SITE_ID;
  const token  = process.env.NETLIFY_API_TOKEN 



BLOBS_STORE_NAME=sityps
NETLIFY_SITE_ID=d8e36c38-1378-4209-aaa0-c1d434c1fb6b
NETLIFY_API_TOKEN=nfp_BfKFKfnaYzaZBUTjzYBFxULjybK1zh7ac0cc








